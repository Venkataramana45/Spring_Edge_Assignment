import 'package:flutter/material.dart';

class NoSize extends StatelessWidget {
  const NoSize({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Please open website in laptop screen"),
      ),
    );
  }
}
